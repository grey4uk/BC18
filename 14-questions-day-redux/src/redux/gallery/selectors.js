export const galleryItemsSelector = (state) =>
  state.images.items;
export const galleryLoadingSelector = (state) =>
  state.images.loading;
export const galleryErrorSelector = (state) =>
  state.images.error;
